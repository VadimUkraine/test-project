import * as React from 'react';
import TodoList from './pages/TodoList';

export const App: React.FC = () => {
    return (
        <div className="test">
            <TodoList/>
        </div>
    );
};

export default App;

